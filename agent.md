# Nimbus VFX Agent Instructions

Canonical detailed instructions live in `AGENTS.md`.

For Nimbus VFX work, use the `Skylord2121` GitHub account only. Do not create, push, commit, or connect Nimbus site work under `danielcallaghan`. The long-term source of truth must be a React + TypeScript + Tailwind app deployed to `nimbusvfx.com` from a Skylord-owned GitHub repository through Vercel, not manually patched standalone HTML.
